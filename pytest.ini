[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short -m "not slow"

markers =
    slow: end-to-end tests that load real model weights (~30 s startup)
    integration: tests against real API endpoints (may require running server)
